document.getElementById("<%= @email.id %>").remove();
document.getElementById("number").innerText = "<%= Email.all.size %>";
$(document.getElementById("<%= @email.id%>")).fadeOut(500).classList.add("destroy_content");

